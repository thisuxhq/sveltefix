{
  "name": "sveltefix-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:toolbar": "bun --filter @thisux/sveltefix dev",
    "dev:mcp": "bun --filter @thisux/sveltefix-mcp dev",
    "build": "bun --filter @thisux/sveltefix build && bun --filter @thisux/sveltefix-mcp build",
    "lint": "bun --filter '*' lint",
    "publish:toolbar": "bun run build && cd packages/toolbar && bun publish --access public",
    "publish:mcp": "bun run build && cd packages/mcp && bun publish --access public",
    "publish:all": "bun run build && cd packages/toolbar && bun publish --access public && cd ../mcp && bun publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
